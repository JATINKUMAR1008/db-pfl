"use strict";
// import { Router } from "express";
// import { createPod, deletePod, getPods } from "../controllers/kube.controller";
// import { validateAuthToken } from "../middlewares/auth.middleware";
// export const kubeRouter = Router();
// kubeRouter.get("/", validateAuthToken, getPods);
// kubeRouter.post("/createPod", validateAuthToken, createPod);
// kubeRouter.delete("/", validateAuthToken, deletePod);
